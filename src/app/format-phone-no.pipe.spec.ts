import { FormatPhoneNoPipe } from './format-phone-no.pipe';

describe('FormatPhoneNoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPhoneNoPipe();
    expect(pipe).toBeTruthy();
  });
});
